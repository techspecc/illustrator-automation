# Illustrator Script: Select Circles by Diameter or Range

This script (`SelectCirclesByRange.jsx`) for Adobe Illustrator allows you to **select all circles** (including symbol instances) based on their diameter.